# GEMINI: do not use this .gitignore file! it is not for you, it is for committing changes to markdown files
#         the files you need to process are ignored by this .gitignore

# Ignore everything
*

# Allow directories so unignored files in subdirectories are reachable
!*/

# Allow Markdown files in root and in any subdirectory
## Allow only markdown files in repository root
!/*.md

# Allow the .gitignore itself so it can be committed
!.gitignore
